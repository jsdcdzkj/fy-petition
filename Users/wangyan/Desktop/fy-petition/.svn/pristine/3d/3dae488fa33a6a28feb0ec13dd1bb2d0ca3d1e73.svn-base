package com.jsdc.petition.dao.sys_;

import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.jsdc.petition.sys_.DivorceProperty;
import org.springframework.stereotype.Repository;

@Repository
public class DivorcePropertyDao {


    /**
     * 此方法为默认方法 请添加自己的逻辑代码
     * @param page
     * @param bean
     * @return
     */
    public String getEntityList(Page page, DivorceProperty bean) {
        String sql = " SELECT *  "+
                " FROM divorce_property  "+
                " WHERE "+ 
                " 1=1 ";

        return sql;
    }
}
